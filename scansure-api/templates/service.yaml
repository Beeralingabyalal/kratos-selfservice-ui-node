apiVersion: v1
kind: Service
metadata:
  name: {{ include "scansure-api.fullname" . }}
  labels:
    {{- include "scansure-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scansure-api.selectorLabels" . | nindent 4 }}
