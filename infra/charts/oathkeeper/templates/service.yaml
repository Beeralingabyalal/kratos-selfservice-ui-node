apiVersion: v1
kind: Service
metadata:
  name: oathkeeper
spec:
  selector:
    app: oathkeeper
  ports:
    - name: proxy
      port: 4455
      targetPort: 4455
    - name: api
      port: 4456
      targetPort: 4456
  type: ClusterIP