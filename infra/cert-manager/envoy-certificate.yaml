apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: envoy-tls
  namespace: authz
spec:
  secretName: envoy-tls
  issuerRef:
    name: scansure-ca
    kind: ClusterIssuer
  dnsNames:
    - envoy.authz.svc.cluster.local